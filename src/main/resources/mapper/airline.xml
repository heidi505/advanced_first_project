<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
        namespace="com.tenco.team_two_flight_ticket._middle._repository.AirlineRepository">
    <select id="findAirlineName" resultType="string">
        select a.name from iata_airline_tb as a where code = #{code}
    </select>
</mapper>