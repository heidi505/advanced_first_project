<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
    namespace="com.tenco.team_two_flight_ticket.reservation.RservationRepository">
   	<select id="getMyTravel">
   		select reservationNum, isPayed, paseengerAmount, paymentDeadline, createdAt, reservationPrice
   		from reservation
   		where id = #{id}
   		and statuseEnum = #{statusEnum}
   	</select>
</mapper>